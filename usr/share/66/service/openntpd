[main]
@type = classic
@description = "OpenNTPD"
@user = ( root )
@options = ( log env )

[start]
@build = custom
@shebang = "/bin/sh"
@execute = ( exec openntpd -d ${OPTS} 2>&1 )

[environment]
OPTS=-s

