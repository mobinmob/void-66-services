[main]
@type = longrun
@description = "apache http daemon"
@version = 0.1.0 
@user = ( root )
@options = ( log )

[start]
@build = custom
@shebang = "/bin/sh"
@execute = ( [ ! -d /run/httpd ] && install -d -m0710 -o root -g _apache /run/httpd 
exec exec httpd -DNO_DETACH )

