[main]
@type = classic
@description = "rsync daemon"
@version = 0.0.3
@user = ( root )

[start]
@build = custom

@execute = (#!/bin/sh  exec 2>&1
 [ ! -e /etc/rsyncd.conf ] && exit 1
exec rsync --daemon --no-detach )
