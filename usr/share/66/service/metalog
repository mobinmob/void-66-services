[main]
@type = classic
@description = "metalog, alternative syslog daemon"
@version = 0.1.0 
@user = ( root )
@options = ( log env )

[start]
@build = custom
@shebang = "/bin/sh"
@execute = ( exec metalog ${OPTS} )

[environment]
OPTS=-v
