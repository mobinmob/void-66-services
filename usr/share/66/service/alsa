[main]
@type = oneshot
@version = 0.0.1
@description = "Restore and store sound card state"
@user = ( root )
@options = ( log )

[start]
@execute = ( alsactl restore )

[stop]
@execute = ( alsactl store )
