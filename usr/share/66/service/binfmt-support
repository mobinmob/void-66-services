[main]
@type = oneshot
@version = 0.0.1
@description = "Start binfmt support"
@user = ( root )
@options = ( log )

[start]
@execute = ( update-binfmts --enable )

[stop]
@execute = ( update-binfmts --disable )
