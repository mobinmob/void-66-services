[main]
@type = classic
@version = @VERSION@
@description = "UUID generation daemon"
@user = ( root )
@options = ( log )

[start]
@execute = (
        execl-toc -d /run/uuidd -g _uuidd -u uuidd
        s6-setuidgid _uuidd uuidd -F -P
 )

