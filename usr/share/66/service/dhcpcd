[main]
@type = classic
@version = @VERSION@
@description = "dhcpcd daemon"
@user = ( root )
@options = ( log env )

[start]
@execute = ( execl-cmdline -s { dhcpcd -f -B ${cmd_args} } )

[stop]
@execute = ( dhcpcd -x )

[environment]
cmd_args=!-M
