[main]
@type = classic
@description = "busybox ntp daemon"
@user = ( root )
@options = ( log env )

[start]
@build = custom
@shebang = "/bin/sh"
@execute = ( exec busybox ntpd -n ${OPTS} )

[environment]
OPTS=
